FROM local/agent-base:2026-05-12

USER root
# Add project-specific runtimes here, e.g.:
# RUN apt-get update && apt-get install -y openjdk-21-jdk maven nodejs npm \
#  && rm -rf /var/lib/apt/lists/*
USER agent
WORKDIR /workspace
