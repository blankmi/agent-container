ARG BASE_IMAGE
FROM ${BASE_IMAGE}

USER root
# Add project-specific runtimes here, e.g.:
# RUN apt-get update && apt-get install -y openjdk-21-jdk maven \
#  && rm -rf /var/lib/apt/lists/*
USER agent
WORKDIR /workspace
