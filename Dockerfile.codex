ARG BASE_DATE
FROM local/agent-base:${BASE_DATE}

USER root
RUN npm install -g @openai/codex
USER agent

# Mount point for the shared codex credentials volume — owned by agent
RUN mkdir -p /home/agent/.codex

CMD ["codex"]
